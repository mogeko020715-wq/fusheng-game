# 动画批次规划（P2 及以后）

> 本文档按优先级排布后续所有动画素材批次，每批含：素材清单、顶替现状、接入点、验收标准。
> 排序原则：**触发频率 × 当前伪装的生硬程度**。
> 所有批次沿用 P0/P1 规范：即梦垫图出图（垫图用对应体型的 stand 帧）+ 通用尾句 +
> `actor-ingest-p1.py` 改 CFG 入库 + `actor-fill.py` 剪影填充 + QC 图 + Playwright 实机验证。

---

## 现状资产（18 帧，已全部接入）

| 类型 | 帧 | 规格 |
|---|---|---|
| 情绪静态 | stand / happy / weak / sick | 画布 900×2150，内容高 1800，脚底锚点 20px |
| 动作静态 | sit / eat | 内容高 1020（坐姿头身比） |
| 动作静态 | sleep | 横版画布 2150×900，内容宽 1650 |
| 帧序列 | walk ×6 / jump ×5 | 内容高 1800 |

当前顶替关系（生硬程度从高到低）：
celebrate→jump 顶替、trip/startle→纯 CSS 摇晃、run→walk 加速、
wash/cook/chat/artist/cure/checkup→stand+CSS、age-m/age-l→幼年帧缩放（最出戏）。

---

## P2 · 事件反馈帧（3 张静态）

> 事件系统是"浮生"情绪起落最集中的地方，目前好结果/坏结果的反馈最单薄。

| 帧 | 顶替现状 | 触发 |
|---|---|---|
| `celebrate.png` 欢呼（双臂高举、踮脚、大笑） | jump 帧顶替 | 事件好结果、心愿达成、生日 |
| `trip.png` 摔倒（坐地、揉眼哭或龇牙） | CSS tripFall 摇晃 | 事件坏结果、运动失败 |
| `startle.png` 受惊（后仰、双臂张开、瞪眼） | CSS startleJump | 打针、惊吓类事件 |

- **接入**：`spriteHold(name, 1200~1600ms)`，无需新机制；celebrate 可叠加现有跳星 fx
- **验收**：三张与 stand 帧发型/服装一致；trip 是"坐地上"（与 sit 盘腿读书区分：腿前伸、手撑地）
- **提示词**：写完本文档后可立即出（结构同 P1，半天工作量）

## P3 · 日常补足（4 张静态 + 4 帧序列）

| 帧 | 顶替现状 | 触发频率 |
|---|---|---|
| `run/` 跑步 4 帧序列 | walk 六帧 95ms 加速 | 高：上课/逃课/运动每天触发 |
| `wash.png` 洗漱（站、手拿杯子刷牙） | stand+CSS 点头 | 高：每日 |
| `chat.png` 聊天（坐姿、手比划、嘴张开笑） | sit 帧顶替 | 中：和家人聊天 |
| `cook.png` 做饭（站、手拿锅铲） | stand+CSS | 低 |

- **跑步序列**：与 walk 同一垫图链技法（逐帧垫上一帧），4 帧一循环即可（walk 的一半密度），
  验收标准同 walk：头顶高度一致、左右腿交替；入库后 `run` 分支从 walk 切到 run 帧组
- **接入**：run 换 `SPRITE_SETS.run`；其余 `spriteHold`

## P4 · 少年体型（7–12 岁，8 张起步）

> **所有批次里对观感提升最大的一批**。age-m 现在靠 ×0.9 缩放幼年帧，
> 是个"放大的一岁娃"，小学六年（游戏约 1/3 时长）全程出戏。

- **体型设定**：5~6 头身、校服感（先在即梦出一张三体型设定图迭代确认，再走帧）
- **首批 8 帧**：stand / happy / weak / sit / eat / sleep / walk×2（关键帧，中间帧可后补）
- **第二批**：walk 补齐 6 帧 + jump×5 + sick + P2/P3 帧（按需）
- **接入**：
  - 目录改 `assets/actor/sprites-kid/`（幼年）+ `assets/actor/sprites-teen/`（少年）
  - 预载策略：两套一起预载（36 张约 3~4MB，可接受）或按年龄懒加载
  - `spriteSet` 前加体型前缀路由；CSS `--asc` 缩放体系退役（同头身比不再需要缩放）
- **画布规格不变**（900×2150 内容 1800），代码零改动

## P5 · 青年/成年体型（13 岁+，同 P4 结构）

- 7 头身，age-l ×1.12 的大头娃娃是终局观感的最大短板
- 帧清单同 P4；可只做到 stand/walk/jump/sit/eat/sleep 核心 6 类
- **依赖**：P4 管线跑通后纯复制，边际成本低

## P6 · 远期可选（不计入排期）

- 结局帧：夭（躺倒花）/ 成年（背影或挥手），用于人生长卷收尾
- 特殊事件帧：生日蛋糕、考试奋笔、毕业
- 性别差分（如未来想做）
- 场景互动差分：课桌版 sit（学校）、鱼竿版 sit（公园）——目前靠 SVG 道具层伪装，立绘模式已不吃这套

---

## 每批执行 checklist（固定流程）

1. [ ] 提示词文档（垫图规定 + 通用尾句 + 验收要点）→ 用户即梦出图
2. [ ] 目检：发型/服装/鞋子与 stand 一致
3. [ ] `actor-ingest-p1.py` 改 CFG 入库 → `actor-fill.py` 填充 → QC 拼图目检
4. [ ] game.js 接入（新动作映射 / 新体型路由）
5. [ ] Playwright 桌面+移动双端验证截图 → git 存档

# 《浮生 · 众生一梦》数值与事件系统体检报告

> 日期：2026-09-16
> 方法：Node 无头模拟，「普通玩家策略」（饭点吃饭、脏了就洗、困了就睡、学龄期 80% 出勤、娱乐不足就玩、其余时间学习/锻炼/探索）连玩 200 世，采集终局数据。
> 工具：`stats-run.js`（可重复运行，改策略即可测其他玩法）

---

## 一、模拟数据总览（200 世）

| 指标 | 结果 | 评价 |
|---|---|---|
| 早夭率 | ≈0% | ✅ 生死压力恰到好处 |
| 中考上重点高中 | **200/200（100%）** | 🔴 完全失去区分度 |
| 智力终值 | **平均 100（全员封顶）** | 🔴 属性通胀 |
| 体质终值 | 平均 73 | 🟡 偏高 |
| 魅力终值 | 平均 85 | 🟡 偏高 |
| 「文武双全」标签（体质+智力≥75） | 96/200 | 🔴  inflation 佐证 |
| 「身怀绝技」（技艺≥5级） | 118/200 | ✅ 合理的培养目标 |
| 家庭暗线完成率 | 100% | ✅ 保底调度有效 |
| 心愿触发 / 达成 | 100% / 100%（模拟总选第一选项） | 🟡 达成条件偏松 |
| 幸福均值 | 63~70 | 🟡 见 P1-1 |
| 「快乐童年」（幸福≥70） | **2/200** | 🔴 阈值失真 |
| 「无忧无虑」（富贵+幸福≥65） | 34/200 | 🟡 基本只看出生 |
| 随机事件触发 | ≈35~45 次/世 | 🟡 偏频繁（已调） |
| 最频繁事件 | 「突如其来的雨」5 次/世 | 🔴 无条件限制（已修） |

---

## 二、问题清单（按优先级）

### 🔴 P0-1 属性通胀 —— 结局失去区分度的总根源

**现象**：上课 `智力 +1.1~1.5`、看书 `+1.2×学习资质`，收益恒定、90 个游戏日内可刷数百次，智力必然封顶 100；中考公式 `智力×0.8 + 体质×0.1 + 心情×0.2 + rand(25) ≥ 78` 在智力 80+ 时失去意义。

**连锁后果**：中考 100% 重点 → 「金榜题名」人人都有 → 判词/标签/结局同质化 → 心愿「科学家」（智力≥75）人人达成。

**建议方案：边际递减成长**

```js
// gain() 内加入衰减因子：属性越高越难涨，软上限约 92
function gain(attr, v) {
  const mul = S.buffs.reduce((m, b) => m * (b.gainMul || 1), 1);
  const da = S.flags.dream && DREAMS[S.flags.dream] && DREAMS[S.flags.dream].attr;
  const dreamMul = (da === attr && v > 0) ? 1.1 : 1;
  const dim = v > 0 ? Math.max(0.25, 1 - S.attrs[attr] / 120) : 1; // ← 新增
  const before = S.attrs[attr];
  S.attrs[attr] = clamp(round1(S.attrs[attr] + v * mul * dreamMul * dim), 1, 100);
  fx(attr, S.attrs[attr] - before);
}
```

预期效果：认真学习者的智力收敛在 85~95，普通玩家 60~75，摆烂玩家 40 上下；中考三档自然分流。若改动后中考重点率仍 >50%，可把门槛从 78 提到 82~85。

### 🔴 P0-2 事件 weight 字段失效

**位置**：`drawEvent()` —— `pool.sort(按 weight)` 后 `pick(pool)` 均匀抽取，**排序不改变概率**，weight 形同虚设。

**佐证**：`weight: 0.4` 的「赠书老者」出现 824 次（≈4 次/世），与无条件事件同频；`weight: 3~5` 的暗线事件毫无优势（目前靠 `checkFamilyArc` 保底调度兜底才没出事）。

**修复（加权抽样）**：

```js
function drawEvent() {
  const pool = EVENTS.filter((e) => {
    if (S.age < e.min || S.age > e.max) return false;
    if (e.cond && !e.cond(S)) return false;
    return true;
  });
  if (!pool.length) return null;
  let total = 0;
  const weights = pool.map((e) => { total += (e.weight || 1); return total; });
  const r = Math.random() * total;
  for (let i = 0; i < pool.length; i++) if (r < weights[i]) return pool[i];
  return pool[pool.length - 1];
}
```

weight 生效后建议复查各事件 weight：日常小事件 0.5~1，剧情事件 2~3，暗线 3~5。

### 🟡 P1-1 「快乐童年」阈值失真

幸福均值被「上学 -3 / 学习 -6 娱乐」压在 67 上下，≥70 仅 2/200。
**建议**：阈值降到 65；或增强正向心情来源（「被爱环绕」持续期间心情缓慢 +1/时段）。

### 🟡 P1-2 「无忧无虑」太易得

富贵出生率 17% × 幸福≥65（≈人人可达）≈ 34/200，基本只看出身。
**建议**：阈值提到 72+，让它成为「富贵且真正被爱」的专属标签。

### 🟡 P1-3 心愿达成条件偏松

「科学家」智力≥75 在通胀环境下人人达标。P0-1 修复后可顺带收紧：
`science` 改为 `智力 ≥ 85` 或（重点高中 **且** 智力 ≥ 75）。

---

## 三、已修复记录（本次）

| 项目 | 改动 |
|---|---|
| 名字库 | 删除「招娣」，换「望舒」（第二次修复，注意外部编辑回退问题） |
| 「突如其来的雨」 | 加 `cond`：仅户外、白天（slot ≤ 4）触发；weight 0.6 |
| 事件频率 | 触发率 0.11 → 0.085，冷却 4 → 5 时段（≈每 14 个动作一次） |
| **存档系统** | 每回合自动存 localStorage；扉页「继续上一世」按钮；事件弹窗状态可恢复；落幕/新生自动清档 |

## 四、存档系统设计备忘

- Key：`fusheng_save_v1`，每次 `render()` 自动写入（状态体 < 3KB）
- 存：完整 `S` + 打开中的事件 id + 里程碑队列 id + 版本号
- 恢复：`resumeLife()` 重建 S → 若有未完成事件则重开该事件，否则继续里程碑队列
- 清除：endLife（落幕/早夭）、startLife（主动新生）
- 原则：**不允许读档重来，只允许中场休息**——与原作精神一致

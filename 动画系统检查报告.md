# 动画系统检查报告

> 检查日期：2026-09-21 · 检查范围：素材资产 / 动作映射 / 状态机 / 兼容与回退
> 方法：18 帧像素级体检 + 代码全路径核对 + Playwright 六项压力测试（桌面 1280 + 移动 390 此前已验）
> 结论先行：**系统整体健康，两处暗伤已修复并提交（`3689b6a`），可以进 P2。**

---

## 1. 素材体检（18 帧，全部合格）

当前线上规格：立绘 `180×430`、横躺帧 `430×180`（经 `958d0ce` 瘦身，7.2MB→464KB，灰度化）。
实测总重 **596KB / 18 帧**，移动端预载无压力。

| 检查项 | 结果 |
|---|---|
| 画布尺寸一致 | ✅ 站立系统一 180×430，sleep 430×180（横版特例，CSS `f-sleep` 按宽度基准） |
| 脚底/底边锚点 | ✅ 全部底边距 4px（瘦身前 20px 等比） |
| 水平居中 | ✅ 偏差 ≤1px |
| 头发区填充 | ✅ 各帧覆盖率 0.39~0.86；walk 后半段透明已修（`ce315c0`）。sit/eat 0.39~0.43 是发型本身收口小，非缺陷 |
| 线条颜色 | ✅ 嘴部浅灰线已提深度（`ce315c0`），各帧嘴型可读 |
| 内容高度 | ✅ 站立系 ~360（同头身比），坐姿 ~204，sick 弯腰 ~307，sleep 体长 ~328 宽 |

## 2. 动作映射（27 个游戏动作 + 3 个事件动作，全覆盖）

| 视觉路径 | 动作 | 状态 |
|---|---|---|
| walk 六帧 150ms | walk/chore/veg/carry/book + 换场景 | ✅ |
| walk 六帧 95ms（加速） | class/skip（run） | ⚠️ 顶替，P3 专用跑步帧已排期 |
| jump 五帧 190ms | play/exercise/slide/toy/武术art | ✅ |
| jump 五帧（顶替） | celebrate（事件好结果） | ⚠️ 顶替，P2 欢呼帧已排期 |
| sleep 横躺帧 + 时间轴 | sleep | ✅ |
| eat 静态帧 1.6s | meal/snack/deli | ✅ |
| sit 静态帧 1.6s | study/art | ✅ |
| sit 静态帧（语义借用） | chat/fish/watch/chess/club | ⚠️ 聊天/钓鱼/下棋都显示"看书"，P3 chat 帧已排期，fish/chess/watch 远期差分 |
| stand + CSS 点头 | wash/cook/artist/cure/checkup | ✅ |
| CSS 表演（无专帧） | trip（事件失败）/startle（戳一戳） | ⚠️ P2 已排期 |
| 情绪帧 | stand/happy/weak/sick（感冒） | ✅ 优先级 感冒>虚弱低落>开心>平常 |
| 年龄缩放 | age-s .72 / age-m .9 / age-l 1.12 | ⚠️ 幼年帧缩放硬撑，P4/P5 体型已排期 |
| 夜晚 | invert(.92) 全帧适用 | ✅ |

## 3. 本次发现并修复（已提交 `3689b6a`）

**① 戳一戳失效（功能级）**：点击监听挂在 `svg#actor` 上，立绘模式下它 `display:none` 永远点不到；
立绘层 `pointer-events:none`。→ 监听双挂 `#actor` + `#actor-sprite-wrap`，立绘层开启点击。
实测：点击立绘 → `anim-startle` 触发 ✅。

**② ps-lie/ps-sit 姿态类残留（卫生级）**：sleep 分支早退，class 清理定时器不执行，
`ps-lie` 永久残留（不可见但属脏状态）。→ SVG 姿态类挪入 class 流程内，仅回退模式使用。
顺带实测 SVG 回退模式（强制禁用立绘）：姿态类与动画正常 ✅。

## 4. 状态机压力测试（6/6 通过，零 JS 报错）

| 用例 | 结果 |
|---|---|
| T1 戳一戳（修复后） | ✅ anim-startle 正常触发 |
| T2 六动作无间隔连击（sleep/walk/eat/jump/sleep/run） | ✅ 1.5s 归位 stand，无残留定时器打架 |
| T3 睡觉中途感冒 | ✅ 起身自动切 sick 帧 |
| T4 跳跃中途心情转好 | ✅ 落地自动切 happy 帧（播放期间不被打断） |
| T5 走路中途换场景 | ✅ 无报错，正常落地新场景 |
| T6 睡觉中途再点睡觉 | ✅ 时间轴干净重入，无 f-sleep 残留 |

## 5. 遗留事项（不阻塞，均已排期或建议排期）

1. **管线补齐**：`958d0ce` 的瘦身（灰度+缩至 430px）是手工操作未脚本化，`assets/actor/src/` 仍是原图。
   **建议**：P2 入库前补一个 `actor-slim.py`，固定流程变为 ingest → fill → retouch → slim 四步，
   否则新帧会以 900×2150 混入（功能正常但体积回涨 7 倍）。
2. **SVG 道具层在立绘模式不可见**：cook 蒸汽、fish 鱼竿、art 画板/音符、chat 气泡均失效
   （道具画在隐藏的 svg 内）。主场景已被 eat/sit 帧自带的碗/书覆盖，属可接受损失；远期场景差分时一并考虑。
3. **语义借用的 sit 帧**：chat/fish/watch/chess/club 五个动作共用"看书"帧，其中聊天最出戏 → P3。
4. **年龄体型**：P4（少年）/P5（青年）排期不变，是所有遗留项里观感收益最大的。

## 6. 结论

素材规格统一、27+3 动作全覆盖、状态机六项压力测试全过、回退路径可用。
已修 2 处暗伤，遗留 4 项全部在动画批次规划内有对应排期。**建议下一步：补 `actor-slim.py` 后直接进入 P2 提示词。**

# 立绘素材规格表 · 浮生 / 众生一梦

> 用途：把现在的简笔 SVG 小人升级为立绘风格时，交给画师或 AI 出图的完整规格。
> 原则：**代码驱动层不变**——JS 只切状态类名，素材按本规格命名、切图、对齐锚点即可直接接入。

---

## 0. 先决决策（开工前必须定）

| 决策项 | 选项 | 建议 |
|---|---|---|
| 风格路线 | A. 黑白线稿立绘 / B. 彩色立绘+场景翻新 / C. 彩色人物+线稿场景 | **A**，与现有纸墨场景零冲突，成本最低 |
| 性别 | 男女两套 / 中性一套 | 第一版**中性一套**（短发或遮耳发型），省一半素材 |
| 覆盖范围 | 全状态 / 分阶段 | **分阶段**，见第 4 节优先级 |

---

## 1. 画布与锚点约定（最重要，错了全部返工）

- **统一画布**：宽 600 × 高 800 px，透明背景 PNG。所有图共用此画布，**严禁按内容裁剪**。
- **锚点 = 脚底中心**：水平居中 x=300，脚底贴 y=790（底部留 10px 安全边）。
- **头顶安全区**：最高发丝/道具不超过 y=10。
- **显示尺寸**：**保持 70px**（已拍板：比例优先，可接受轻微发糊）。配套补偿：抠图管线默认"加粗线 + 提 alpha 对比"（`preview-70bold.py`：grey_erosion 13px + alpha 曲线），实测 70px 下轮廓可读。后续若仍嫌糊，可选 85px 中方案或把场景舞台调高，均为零素材成本。
- **填充**：人物皮肤/衣物区域必须**烘焙不透明纸色 `#fbf9f3`**，不能透明——否则场景线条会透过身体（第一版合成实测踩坑）。剪影填色 + 线条叠 alpha 即可。
- **夜晚**：不出夜景素材。白天 PNG 整体 `filter: invert()` 反白，纸底变深、墨线变浅，实测效果成立。
- **注意**：70px 立绘头顶约在舞台 96px 高度，与天体轨迹（太阳最低约 48px）不相碰，无需让位。
- **线宽**：70px 显示约等于画布的 1/11，主线越粗越好、少画发丝衣褶等碎线（即梦提示词加 `bold uniform outline, minimal detail lines`）；残留的细线由抠图管线的加粗步骤兜底。
- **朝向**：一律面朝右前方（3/4 侧面或正面均可，全套统一）。
- **地面参考**：脚下地面线在 y=790，人物不得悬空（跳跃帧除外，见 3.2）。

```
┌─────────────────────┐ y=0
│   ← 头顶安全区 10px →│
│                     │
│      （人物）        │  人物中线 x=300
│                     │
│                     │
├─────────●───────────┤ y=790 ← 锚点：脚底中心
└─────────────────────┘ y=800
```

---

## 2. 素材矩阵

### 2.1 基础立绘（静态图）

年龄档对应代码：`age-s`（3-6 岁）/ `age-m`（7-12 岁）/ `age-l`（13-18 岁）。
注意：**不能一张图缩放三档**——体型比例要重画（3 岁约 4 头身，7-12 约 5 头身，13-18 约 6 头身）。

| 编号 | 内容 | 数量 | 说明 |
|---|---|---|---|
| B-s-stand | 幼年 · 站姿 | 1 | 好奇、微微仰头 |
| B-s-sit | 幼年 · 坐姿 | 1 | 盘腿/小板凳均可，脚底锚点改为"臀部着地点" |
| B-s-lie | 幼年 · 躺姿 | 1 | 侧卧，头朝左（与现在睡姿 SVG 一致） |
| B-m-stand / sit / lie | 少年 · 三姿势 | 3 | 书包、红领巾等时代元素可选 |
| B-l-stand / sit / lie | 青少年 · 三姿势 | 3 | 接近成人比例 |

小计：**9 张**（3 体型 × 3 姿势）。

### 2.2 表情差分（覆盖在同编号底图上）

代码状态 class：`happy`（心情≥80）/ `weak`（精力或健康低）/ `moodlow`（心情<25）/ `sick`（感冒）。

| 差分 | 对应 class | 数量 | 说明 |
|---|---|---|---|
| 开心 | `happy` | 3（每体型 1） | 眯眼笑，站姿即可 |
| 虚弱 | `weak` | 3 | 驼背、眼皮耷拉 |
| 低落 | `moodlow` | 3 | 嘴角向下、肩膀内收 |
| 生病 | `sick` | 3 | 脸红/鼻涕，可与虚弱合并为一张 |

小计：**9–12 张**。若做差分图层（只换脸），每张底图配 4 张脸部 PNG 亦可，二选一。

### 2.3 手中道具（沿用现有简笔道具层，可暂缓）

鱼竿 / 书 / 音符 / 画架 / 热气 / 对话气泡 6 种，代码 class `p-rod` `p-book` `p-notes` `p-easel` `p-steam` `p-bubble`。
**建议保留现在的线稿道具叠加层**，立绘接入后检查遮挡即可，不必重画。

---

## 3. 动作帧（精灵图）

代码动画 class 共 10 种：`anim-walk` `anim-run` `anim-jump` `anim-stand` `anim-sit` `anim-eat` `anim-sleep` `anim-startle` `anim-celebrate` `anim-trip`。
其中 `sit / eat / sleep / stand` 由静态姿势图 + CSS 微动承担，**不需要帧序列**。

### 3.1 需要帧序列的 6 个动作

| 动作 | class | 帧数 | 时长 | 说明 |
|---|---|---|---|---|
| 走路 | `anim-walk` | 6 | 0.9s 循环 3 次 | 标准步行循环：触地→过渡→抬腿→跨步 |
| 跑步 | `anim-run` | 4 | 0.85s | 可用走路帧加速+前倾代替，第一版可省 |
| 跳跃 | `anim-jump` | 5 | 0.95s 单次 | 蓄力下蹲→起跳→腾空→落地压扁→回弹 |
| 吓一跳 | `anim-startle` | 4 | 0.6s 单次 | 后仰跳起，头发竖起 |
| 庆祝 | `anim-celebrate` | 5 | 1.05s 单次 | 跳起+双臂上举 |
| 摔倒 | `anim-trip` | 4 | 1.0s 单次 | 前扑→倒地→撑起 |

**只做幼年一套**（P0，见第 4 节），验证效果后再补少年/青少年。

### 3.2 精灵图排布

- 横向单排：单帧 600×800，N 帧拼成宽 600×N 的一行 PNG；
- 跳跃/腾空帧允许脚底离开 y=790（腾空感），但**起跳和落地帧必须贴回锚点**；
- 命名：`A-s-walk.png`（幼年走路）… 规则见第 5 节。

---

## 4. 分期优先级

| 期 | 内容 | 数量 |
|---|---|---|
| **P0**（先跑通） | 幼年站姿底图 + 开心/虚弱 2 表情 + 走路 6 帧 + 跳跃 5 帧 | 约 14 张 |
| **P1** | 幼年坐/躺 + 剩余表情 + 庆祝/摔倒/吓一跳帧 | 约 18 张 |
| **P2** | 少年、青少年全套复制 | 约 32 张 |
| **P3**（可选） | 男女分化、跑步独立帧、道具重绘 | 视情况 |

---

## 5. 命名与交付规范

```
assets/actor/
  B-s-stand.png            基础立绘：B-{age}-{pose}
  B-s-stand@happy.png      表情差分：@{mood}，无后缀=平常
  A-s-walk.png             动作帧：A-{age}-{anim}（横向单排精灵图）
  A-s-walk.meta.txt        帧数与建议时长（一行：6 0.3）
age  ∈ s | m | l
pose ∈ stand | sit | lie
mood ∈ happy | weak | moodlow | sick
anim ∈ walk | run | jump | startle | celebrate | trip
```

- 格式：PNG-24 透明背景；全套导出后用 ImageOptim/oxipng 压缩；
- 总量控制：P0 全套 ≤ 2 MB（游戏主打零依赖秒开）；
- 线稿色值：主线 `#1c1c1c`，辅助线 `#8a867d`，点缀朱砂 `#b3382c`（与场景 CSS 变量一致）；若走彩色路线另定色板。

---

## 6. AI 出图提示（若不用画师）

- 先出一张**角色设定稿**（三体型并排、统一画风），后续所有图以它为参考图垫图，保住一致性；
- 提示词骨架：`children's book ink illustration, black and white line art on plain background, full body, {age} years old kid, {pose}, minimalist strokes, flat, no shading, transparent background`；
- 帧序列不要指望 AI 直接出——出关键帧后人工/工具修对齐，或以走路循环参考图逐帧垫图。

---

## 7. 接入 checklist（素材到货后）

1. [ ] 全部图锚点抽检：脚底中心偏差 ≤ 4 px
2. [ ] `index.html`：`#actor` 换为帧动画容器，保留 `#actor-fx` 情绪气泡层
3. [ ] `style.css`：重写 actor 动画段（`background-position` + `steps()`）
4. [ ] `game.js`：`render()` 年龄档切图片组；`ACTOR_SPOT` 按立绘脚底重新标定
5. [ ] 六场景 × 六时段截图回归（沿用 `shot8-verify.js` 流程）
